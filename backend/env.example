APP_ENV=local
DATABASE_URL=sqlite:///./data/app.db
CORS_ORIGINS=http://localhost:5173

# AI mode: "mock" or "llm"
AI_MODE=mock

# Optional if AI_MODE=llm (OpenAI-compatible)
LLM_PROVIDER=openai
OPENAI_API_KEY=REPLACE_ME
OPENAI_MODEL=gpt-4o-mini

